import { Box } from '@mui/material';

const DeseaseCard = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        flexDirection: 'column',
      }}
    >
      DeseaseCard
    </Box>
  );
};
export default DeseaseCard;

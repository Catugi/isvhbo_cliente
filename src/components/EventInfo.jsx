import { Card } from "@mui/material";
import { grey } from "@mui/material/colors";

const style = {
  width: 600,
  height: 500,
  bgcolor: grey[400],
};

const EventInfo = () => {
  return <Card>EventInfo</Card>;
};

export default EventInfo;
